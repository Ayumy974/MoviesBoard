import React from 'react';
import MovieCard from './movieCard/MovieCard';

const MoviesList = () => {
    return (
        <div>
            <MovieCard />
        </div>
    )
}

export default MoviesList
